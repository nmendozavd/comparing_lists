
def compare_lists(list_one, list_two):
    if list_one == list_two:
        print "The lists are the same"
    else:
        print "The list are not the same"

print compare_lists(list_one = [1, 2, 5, 6, 2],list_two = [2, 2, 5, 6, 2])
